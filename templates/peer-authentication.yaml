{{- if .Values.istio.enabled }}
apiVersion: security.istio.io/v1beta1
kind: PeerAuthentication
metadata:
  name: {{ include "support-service.fullname" . }}-mtls
  labels:
    {{- include "support-service.labels" . | nindent 4 }}
spec:
  selector:
    matchLabels:
      {{- include "support-service.selectorLabels" . | nindent 6 }}
  mtls:
    mode: {{ .Values.istio.mtls.mode | default "STRICT" }}
{{- end }}
